import { PlaceholderScreen } from '../../src/components/placeholder-screen';
export default function LoginScreen() { return <PlaceholderScreen title="Welcome back" subtitle="Email, Google, and session restoration are routed through the API auth boundary." />; }
