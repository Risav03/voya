export const prompts = {
  reelUnderstanding: `Extract structured travel intent from captions, transcripts, OCR, and visual evidence. Return only JSON matching the schema.`,
  itineraryGeneration: `Create an experience-first itinerary that balances saved intent, route efficiency, open hours, fatigue, backups, and live context. Return only JSON matching the schema.`,
  dailySummary: `Write a concise personal travel memory from check-ins, photos, notes, and route progress. Return only JSON matching the schema.`,
};
